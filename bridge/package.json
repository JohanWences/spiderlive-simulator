{
  "name": "spiderlive-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Modbus TCP slave <-> WebSocket bridge connecting OpenPLC (master) to the SpiderLive web simulator (virtual plant)",
  "scripts": {
    "start": "node index.js",
    "mock": "node mock-plant.js"
  },
  "dependencies": {
    "modbus-serial": "^8.0.17",
    "ws": "^8.18.0"
  }
}
